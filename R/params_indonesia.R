# Parameters derived from EM-DAT + IMF — dummy values for now

gdp_init <- 100
debt_init <- 60
rev_ratio <- 0.14
spend_ratio <- 0.18

shock_prob <- 0.4             # ~1-in-3-year major disaster
shock_loss_pct <- 0.06           # 6% GDP loss per event
emergency_spend_pct <- 0.04    # 4% fiscal response

adaptation_cost_pct <- 0.01      # 1% of GDP per year for 5 years
adaptation_years <- 5
adaptation_effectiveness <- 0.85
